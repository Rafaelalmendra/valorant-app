import styled from "styled-components";

export const CreditsCard = styled.div`
  display: flex;
  flex-direction: column;
  gap: 0.25rem;
  margin-bottom: 3rem;
  text-align: center;
  p {
    font-size: 1rem;
  }
  h3 {
    color: var(--red);
    font-size: 1.25rem;
    font-weight: 500;
  }
`